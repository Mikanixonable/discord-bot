# 新機能 実装計画

これまでの検討で確定した方針のまとめ。実装の指針として使う。

## 全体を貫く設計判断

- **ツール実行はモデルのネイティブfunction callingに頼らず、TypeScript側でオーケストレーションする。**
  この abliterated モデルは `tool_calls:[]` で本文が空になる不具合があり、関数呼び出しが不安定。
  「検索するか判断 → 実行 → 結果を文脈に注入 → 生成」という制御はコード側で書き、モデルにはテキスト生成だけさせる。機能3〜6はこの前提。
- 検索系(#3〜6)は「検索オーケストレータ + 複数ソースアダプタ」という共通土台の上に載せる。先に土台(retrieval + 文脈注入の枠組み)を設計してから各ソースを足す。

---

## 1. 返答を早くする

**結論: 複数レバーで対応。体感改善を優先。**

- **ストリーミング + Discordメッセージ逐次編集**（`stream: true` → 最初の1文で送信、以降1秒間隔で `message.edit()`、編集レート制限 約5回/5秒に合わせてスロットリング）。体感レイテンシへの効果が最大。
- **`max_tokens` 上限**で生成トークンを削る（現状40sの大半は生成時間）。雑談時は「短く返す」指示も併用。
- **プレフィックスキャッシュ活用**: system(persona)を毎回完全一致で固定し、LM StudioのKVキャッシュを効かせる。履歴20件も削減候補。
- 投機的デコードは既に有効（draft acceptance 0.97）。

## 2. Discordカスタム絵文字

**結論: 容易。短縮名を渡し、送信前に完全構文へ置換。**

- `GuildEmojisAndStickers` intent を追加。
- 起動時 + `emojiUpdate` 系イベントで `name → {id, animated}` のMapを構築。
- systemプロンプトに「使用可能な絵文字: :foo: :bar: …」を注入（多ければ抜粋）。
- 送信直前に出力中の `:name:` を `<:name:id>`（アニメは `<a:name:id>`）へ正規表現で置換。

## 3. Web検索（Wikipedia / 公的資料 / Bluesky / 一般Web）

**結論: 一般Web検索は SearXNG(セルフホスト) をデフォルト、Tavily をフォールバック。専用APIは個別アダプタで併用。**

- **一般Web検索**: **SearXNG**（セルフホスト・APIキー不要・完全無料）。`compose.yml` にサービス追加し、botと同じDockerネットワークでJSON API内部通信。`.env` に `SEARXNG_URL`。JSON出力は設定で有効化が必要。
  - フォールバック/精度重視時に **Tavily**（無料1,000クレジット/月・カード不要）。
  - ~~Brave~~ は2026年2月に無料枠廃止・従量課金化したため不採用。
- **Wikipedia**: MediaWiki API（`ja.wikipedia.org/w/api.php`、キー不要）。専用アダプタで高精度。
- **Bluesky**: AT Protocol `app.bsky.feed.searchPosts`（`public.api.bsky.app`、認証不要）。専用アダプタ。
- **公的資料**: SearXNG/Tavily の結果を `go.jp` 等でドメインフィルタ。
- **検索要否の判断**: ネイティブtool callは使わず、ヒューリスティック or 「検索が必要か? 検索語は?」を1行フォーマットで返す軽量1パスでパース。

```
src/tools/search/
  searxng.ts     # 一般Web検索（デフォルト）
  tavily.ts      # フォールバック / 精度重視
  wikipedia.ts   # MediaWiki API
  bluesky.ts     # AT Protocol
  index.ts       # ソース選択・統合オーケストレータ
```

## 4. ローカルPDF/テキスト参照

**結論: 指定ディレクトリへのRAG。まずBM25、必要なら埋め込みへ拡張。**

- 軽量版（スタート）: ファイル読み込み → チャンク分割 → **BM25/キーワード検索**。小規模コーパスに十分、埋め込み不要。
- 本格版: LM Studio の `/v1/embeddings` でベクトル化 → SQLite + `sqlite-vec` かメモリ内コサイン類似度。
- PDF解析: `pdf-parse` か `pdfjs-dist`。
- `src/rag/` にインデクサ（dir監視→解析→チャンク→ストア）と検索器。#3オーケストレータに「ローカル資料」ソースとして統合。

## 5. Discord過去発言の参照（リンク先・同一キーワード）

**結論: 自前インデックス（SQLite FTS5）が必須。リンク解決は単体で先行実装可能。**

- **リンク先/返信元**: メッセージ内のDiscordリンク（URLからID抽出）や `message.reference` を `channel.messages.fetch(id)` で取得。容易。
- **キーワード検索**: Bot APIに全文検索が無いため、受信メッセージを逐次 **SQLite FTS5** に記録し、過去分は履歴fetchでバックフィル。クエリ時にFTS検索。
- `src/history/` にメッセージロガー + 検索器。dev.md の「長期記憶」「話題の整理」「過去類似トピックとの位置づけ」の基盤を兼ねる。

## 6. エクスポート済み過去ツイート

**結論: アーカイブをパースし、#4/#5の検索ストアへ投入。**

- 前提: エクスポートフォルダの再配置（または `.env` でパス指定）。現在は作業ディレクトリに無い。
- Twitterアーカイブの `tweets.js`（`window.YTD... = [ {...} ]` 形式）をパースし `full_text` / `created_at` を抽出。
- 検索ストア（BM25 or 埋め込み）へ投入し「自分の過去の考え」ソースとして関連ツイートを文脈注入。retrieval は #4/#5 のインフラを再利用。

## 7. 内省してから返答する

**結論: 最優先。現在の空応答バグ（`tool_calls:[]` + content空）の恒久対応と一体。**

- まず、いま仕込んだ生レスポンスログで **思考がどのフィールドに出るか確定**（`reasoning_content` か `<think>...</think>` か）。
- Qwen3の思考モード（テンプレートの `enable_thinking` / `/think`）を有効化し、**コード側で思考部を分離、最終回答だけをDiscordへ送る**。思考はデバッグモードでコンソール出力。
- `max_tokens` に余裕を持たせ、思考が回答を食い潰さないようにする。
- （代替）明示的2パス（①考えを整理 ②それを踏まえ返答）。制御しやすいが遅い。

## 8. メンション時のみ返信、それ以外はメンションなしで反応

**結論: 容易。`shouldRespond` にメンション由来フラグを持たせて分岐。**

- **メンション由来** → `message.reply()`（ping抑制は `allowedMentions: { repliedUser: false }`）。
- **allowlist自動反応** → `channel.send()`（リプライ参照なし=通常発言）。
- 変更は `index.ts` の分岐のみ。

---

## 推奨する着手順

1. **#7 内省 + 空応答バグの恒久対応**（今のエラーを止める。生レスポンス確認が前提済み）
2. **#8 返信形式** ・ **#2 絵文字**（小さく即効、UX改善）
3. **#1 高速化（ストリーミング + max_tokens）**（体感改善が大きい）
4. **#5 Discord履歴インデックス（SQLite FTS5）** — #6・長期記憶・話題整理の共通基盤
5. **#4 ローカルRAG** → **#6 ツイート**（#5の検索基盤を再利用）
6. **#3 Web検索**（外部API選定・キー管理・SearXNG構築が絡むので最後）
