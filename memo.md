# memo
過去のAIの解答をメモする。人間のみが記入

---
全体の感想
機能は大きく2系統に分かれています:

能動系(バッチ的) — 日次の話題振り返り、象徴的発言リンク、話題の整理・シリーズ化。定時実行 or トリガー実行で、専用スレッドに投稿する。
受動系(対話的) — コマンドや呼びかけに応じて要約・意見・Web検索・画像生成・雑談。
この2つはアーキテクチャ的にかなり性質が違う(片方はcronジョブ+履歴収集、もう片方はイベントハンドラ)ので、要件定義でも分けて考えるのが良いです。MVPとしてはどちらか一方から始めるのを勧めます。個人的には「日次振り返り」が一番独自性があり、雑談・コマンド系は後付けしやすいので、能動系を先に固めるのが良さそうです。

具体化すべき論点
1. 履歴の収集と保存

「その日の話題」の対象チャンネルはどこまで?(言語情報チャンネルだけ?鯖全体?)
「過去の類似トピックとの位置づけ」をやるには過去ログのDB化(+埋め込みベクトル検索)がほぼ必須。メッセージをどこに、どの期間、どんな形で保存するか。プライバシー的に鯖メンバーの合意も考慮点。
都度Discord APIで遡るのは遅くレート制限もあるので、常時メッセージを購読してDBに溜める設計が現実的。
2. 「話題」の単位をどう切るか
これが技術的に一番難しい部分です。Discordの会話は話題が交錯するので、「ひとまとまりの話題」の抽出はLLMに時間窓ごとのログを渡してセグメント化させることになる。精度の期待値と、失敗したときの見え方(変な切り方をした要約が投稿される)を許容できるか。

3. 「シリーズの編成」の具体像
成果物は何ですか? スレッド? フォーラムチャンネルの投稿? bot管理のインデックスメッセージ? ここが曖昧だと実装が決められないので、「理想の出力例」を手書きで1つ作ってみるのを強く勧めます(日次振り返りも同様)。

4. コマンド系

スラッシュコマンドにするか、メンション応答にするか(両方も可)。雑談は「botスレッド内では全発言に反応」「通常チャンネルではメンション時のみ」あたりが定番。
「価値判断を述べる」はキャラ設定と絡めて面白い機能ですが、鯖の空気を壊すリスクもあるので、どのくらい踏み込むかのポリシーを決めておくと良い。
画像生成・Web検索はそれぞれ外部API(コストと選定)が必要。優先度を付けて後回し候補に。
5. キャラ設定

システムプロンプト1枚で済む話ですが、「振り返り投稿の文体」にもキャラを通すのか、機能系は事務的にするのかは決めどころ。
キャラ設定・プロンプトは設定ファイルに外出しして、コード変更なしで調整できるようにすると運用が楽。
6. 運用面(見落としがちだが重要)

ホスティング先(常時稼働が必要。自宅PC?VPS?Railway/Fly.io等?)
LLM APIのコスト上限(日次要約は毎日確実に課金される。雑談は使われ方次第で膨らむ)
将来の複数鯖対応は「設定をguild IDごとに持つ」構造にだけしておけば十分。今から作り込む必要はない。
提案する次のステップ
日次振り返り・シリーズ編成の理想の出力サンプルを手書きで作る(要件が一気に具体化します)
MVPスコープを決める(例:メッセージ収集+日次要約投稿のみ)
技術選定(discord.js/discord.py、DB、LLM API、ホスティング)
まず「理想の出力例」を一緒に作るところから始めますか?それとも論点のどれかを深掘りしましょうか。