# 開発ノート
人間のみが記入できる


## done
- 内省を行い、解答を検討した後返答する
- メンションされたときだけ返信し、それ以外はメンションをつけずに反応する

- 読み込むコンテキストを過去20投稿から、過去10投稿だが1000文字を超過する場合はそこで打ち切る処理に変更

- 会話が切れる問題、うまく文の境界で切って分割して投稿できないだろうか

現在、出力がトークンのリミットに達すると途中で止まる
会話を止まらせず、かつ高速で返答するために、空白行が現れたらそこで一度区切って投稿し、漸次出力を投稿する形式にしたい。このような実装は可能だろうか


重層的な記憶を作りたい。
文脈として読み込まれる過去10件の投稿に加えて、これまでのやり取りを要約した短期記憶を参照するようにしたい。更に、そうして要約された記憶を更に圧縮した長期記憶を作り、それも参照するようにしたい。
実装はせず世間の他のエージェントの例を調べて実装案を具体化して

- 現在時刻を取得できるようにしたい。

- プロンプトに含まれるdiscord鯖の別の投稿へのリンクを参照し文脈を調べられるようにしたい
claudeと同じように、ステータス文を表示したい。
例えば、toolを使った際は、「●●チャンネルを検索中...」「webを検索中...」などと薄字のステータス文で表示したい。内省中は「思考中...」と表示させたい。表現にも、ユーモアがあるように何パターンかをランダムに表示したい。


## todo

discordbotを作りたい。以下の機能を持つことを考えている
- 文字情報鯖のみで運用、将来的に別の鯖でも動かすかもしれないが未定
- 言語情報チャンネルの特定のスレッド(「bot」等?)で動く。
- その日の話題の振り返り、象徴的な発言へのリンク、ひとまとまりの話題の整理と過去の類似トピックとの位置づけ、話題のシリーズの編成を行う
- 通常チャンネルでもコマンドによって発言を行う。今日の発言を要約したり、価値判断を述べたり、WEBを調べたり、画像生成を行ったり、あるいは雑談に付き合う。
- キャラ設定を持っている

雑談機能
ローカルLLMを用いる
構成は？モデルは？

エラー原因調査
直列化
コンテキストウィンドウの拡大

デバッグモードを作りたい。

git clone
npm ci
npm run dev

モデル変化
bluesky投稿
文字アート投稿


- wikpediaの記事、政府や公的機関の資料、blueskyの投稿を検索できるようにする
- 必要に応じてローカルの特定のディレクトリにある参考資料のpdfやテキストファイルを参照する
- dockerで起動するときnpm run devで起動するときと.envを書き換えなくてもいいようにしたい

webと同じように、同じサーバー内の過去の投稿を検索できるようにしたい。どのようにすればよいだろうか。実装案を考えて。まだ実装しないで。



web検索を強化したい

召喚機能
「bot」スレッドでなくても、namakoをメンションで呼び出すとそのチャンネルでの過去10件の文脈を読み、必要ならば通常と同じようにweb検索などのツールを用いて返答する。

botが使えるツールを追加
- ローカルのツイートアーカイブ検索機能
D:\lab2\reference\twitter-2026-07-22-9fe1e485cf6b9a8ed3b8107daf69ae9d5bd50c98fb1329257165bf350bc14886
D:\lab2\reference\bluesky
にダウンロードしたtwitterの投稿、blueskyの投稿のアーカイブがある。過去のツイート、いいね、投稿を話題に応じて話題の種として調べて検索できるようにして。





- 投稿が行われなくても、

- ある投稿が自分にとって好意的ならスタンプが押せるようにしたい。押せるスタンプは、「🐙」の絵文字


- 思考中系のステータスは、思考時間か量が長いときだけ表示したい。現状毎回表示されている