# 開発ノート
人間のみが記入できる

discordbotを作りたい。以下の機能を持つことを考えている
- 文字情報鯖のみで運用、将来的に別の鯖でも動かすかもしれないが未定
- 言語情報チャンネルの特定のスレッド(「bot」等?)で動く。
- その日の話題の振り返り、象徴的な発言へのリンク、ひとまとまりの話題の整理と過去の類似トピックとの位置づけ、話題のシリーズの編成を行う
- 通常チャンネルでもコマンドによって発言を行う。今日の発言を要約したり、価値判断を述べたり、WEBを調べたり、画像生成を行ったり、あるいは雑談に付き合う。
- キャラ設定を持っている

雑談機能
ローカルLLMを用いる
構成は？モデルは？

エラー原因調査
直列化
コンテキストウィンドウの拡大

デバッグモードを作りたい。

git clone
npm ci
npm run dev

web検索
長期記憶
モデル変化
bluesky投稿
文字アート投稿
メンションなし

高速化

ログイン完了: namako#2709
自動返信チャンネル: 1529153962911993966
メッセージ処理中にエラーが発生しました: Error: LLMから空の応答が返されました。
    at chatCompletionRequest (D:\lab2\discord-bot\src\llm.ts:77:11)
    at process.processTicksAndRejections (node:internal/process/task_queues:95:5)

以下の機能について、実現可能性、実現方法、実装案を提案してほしい。実装はまだ行わないで
- 返答を早くする
- discordサーバーのカスタム絵文字を取得し、使えるようにする
- 知らないこと、最近のことについてはwebを検索し参照する。特にwikpediaの記事、政府や公的機関の資料、blueskyの投稿を検索できるようにする
- 必要に応じてローカルの特定のディレクトリにある参考資料のpdfやテキストファイルを参照する
- リンクされている投稿、同じキーワードが使われている投稿など、discord鯖の過去の発言を参照し文脈を調べられるようにする
- 私のエクスポートした過去のツイート
- 内省を行い、解答を検討した後返答する
- メンションされたときだけ返信し、それ以外はメンションをつけずに反応する

- 読み込むコンテキストを過去20投稿から、過去10投稿だが1000文字を超過する場合はそこで打ち切る処理に変更

- 会話が切れる問題、うまく文の境界で切って分割して投稿できないだろうか